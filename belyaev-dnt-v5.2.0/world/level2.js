/* Belyaev DNT — маркер уровня 2.
 * Регистрируется service worker'ом только когда защита включена
 * и выбран уровень 2. Выполняется в MAIN-мире на document_start
 * непосредственно перед engine.js, поэтому движок читает значение
 * синхронно, без единого асинхронного вызова. */
window.__BDNT_LEVEL__ = 2;
