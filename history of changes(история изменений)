## История версий

### 5.9.0 — прорывные фичи

- **Поведенческий антифингерпринт.** Микрошум координат мыши, таймингов
  клавиатуры и скролла. Ни одно расширение в мире этого не делает.
  Трекеры нового поколения не могут связать сессии по поведению.
- **Отравление трекерных профилей.** Вместо блокировки аналитических
  запросов — подмена параметров (_ga, _fbp, utm_*). Трекер думает,
  что собрал данные, но профиль отравлен случайными значениями.
- **Автообработчик cookie-баннеров.** Автоматически нажимает «Отклонить»
  на GDPR-баннерах 10 основных CMP-платформ (OneTrust, Cookiebot,
  Didomi, Яндекс и др.). Если кнопка отклонения не найдена — баннер
  не трогается.
- **Рейтинг приватности сайта (A–F).** В карточке уведомления показывается
  оценка приватности: от A (0 трекеров) до F (20+ трекеров).
- **Проверка утечек данных.** При вводе email или пароля на сайте, который
  ранее допускал утечку (LinkedIn, Facebook, Adobe, VK, Rambler, Mail.ru
  и 14 других), показывается предупреждение: год утечки, число пострадавших
  аккаунтов, какие данные утекли. Рекомендация использовать уникальный
  пароль и 2FA. Проверка полностью локальная — email никуда не отправляется.
- **Защита форм от кражи до отправки.** Обнаруживает, когда сторонние
  скрипты считывают введённые в форму данные ещё до нажатия «Отправить».
  Это массовая практика маркетинговых платформ, о которой пользователь
  не знает: даже если передумал и не отправил форму, данные уже ушли.
  Плагин показывает, какие именно домены читают ваш ввод.
- Механизм перехвата: подмена геттера `value` для полей email/tel/name
  с анализом стека вызовов — определяется источник чтения.

### 5.8.0 — интеллектуальная адаптация

- **Встроенный тест защиты.** HTML-страница внутри расширения: показывает
  все параметры отпечатка, отмечает подменённые зелёным, проверяет
  поведенческий антифингерпринт при движении мыши. Без внешних сайтов.
- **Контекстно-адаптивная защита.** Определяет тип сайта (банк, CRM,
  соцсеть, маркетплейс, Google Workspace) по DOM и домену. Если текущий
  уровень может ломать сайт — показывает предложение с объяснением и
  кнопкой «Применить рекомендуемый уровень».
- **Умное предложение исключений.** Детектирует признаки поломки сайта
  (JS-ошибки blocked/cors/denied, пустые iframe) и предлагает понизить
  уровень до 1 с одной кнопкой. Перезагружает страницу после применения.
- Все innerHTML с пользовательскими данными заменены на textContent.

### 5.7.0 — режим защиты static/dynamic

- **Переключатель режима защиты.** Статическая (по умолчанию) — стабильный
  профиль на домен, сайты работают ровно. Динамическая — новый профиль при
  каждой загрузке, максимальная защита от связывания сессий.
- Кнопка на главном экране с пояснением когда какой режим лучше.
- Режим сохраняется между сессиями и экспортируется/импортируется.
- mode.js: ISOLATED content-script передаёт режим через data-атрибут
  на <html>, engine.js считывает синхронно.

### 5.6.0 — исправление per-site уровней и UX

**Критичный баг:** per-site уровни не работали — при глобальном уровне 5
и назначении сайту уровня 1 сайт всё равно ломался. Причина: DNR-правила
блокировки трекеров исключали только домены со статусом «off», но не домены
с пониженным уровнем. Теперь все per-site домены добавляются в
`excludedInitiatorDomains`, и DNR-блокировка не применяется к сайтам
с собственным правилом.

- Пояснение к Экспорту/Импорту настроек.
- Несколько сайтов для проверки защиты (BrowserLeaks, Whoer, EFF, Pixelscan).
- Кнопка переключения тёмной/светлой темы.


### 5.4.3 — аудит безопасности

**Исправлено 10 дефектов:**

- XSS-вектор в экране лицензии: innerHTML заменён на DOM-сборку.
- Внешние фильтр-листы: добавлена валидация доменов (regex, длина <255).
- Импорт настроек: добавлена проверка типов данных — нельзя загрузить
  произвольные значения через JSON.
- fetch фильтр-листа: добавлен таймаут 15 сек (AbortController).
- Per-site правила: checkCurrentSite не находил правило для поддоменов —
  исправлено (sub.example.com наследует правило example.com).
- getNoticeState: per-site правила не учитывались для content-скриптов —
  на исключённом сайте реклама всё равно скрывалась (исправлено ранее, 
  проверено повторно).
- PerformanceObserver в notice.js не отключался — утечка ресурсов.
- MutationObserver в passwordtips.js работал бесконечно после показа.
- MutationObserver в adblock.js вызывал sweep на каждую мутацию без
  дебаунса — замедлял динамичные страницы. Добавлен дебаунс 300 мс.
- urlsafety: sessionStorage мог не записаться до reload после «Принимаю
  риск» — добавлена задержка 50 мс.

### 5.5.1

- **Обновляемые фильтр-листы.** Раз в сутки скачивается Peter Lowe's list
  (~3 000 доменов). Подмешивается к встроенным 83 доменам. Кнопка ручного
  обновления на экране «О плагине». При отсутствии сети — встроенные работают.
- **Еженедельный отчёт «Ваша приватность».** Уведомление раз в неделю:
  общий счётчик заблокированного, число трекеров, лидер слежки.
- **Импорт/экспорт настроек.** Экспорт в JSON, импорт из файла — для
  развёртывания на несколько машин. Экран «О плагине» → «Настройки».
- **Встроенный тест защиты.** Кнопка «Проверить защиту» открывает
  BrowserLeaks, где пользователь видит, что подменено.
- Статус фильтр-листов (встроенные + внешние, дата обновления).

### 5.4.2

- **Onboarding** при первой установке (3 слайда в попапе).
- **Бейдж-счётчик** заблокированного на иконке расширения.
- **Тёмная тема** — дополнены недостающие переменные, полная поддержка.
- **Анимированный логотип** — серфингист на глобусе: зелёный щит с
  кирпичиками (ON), красный глобус с акулами (OFF).
- Кнопка «Добавить» в per-site правилах перенесена под поля.
- Лицензия: убрана дата начала, только остаток дней.
- История блокировок: общий счётчик, бейджи «Трекер»/«Реклама».

### 5.4.0

- Аудит кода: исправлены 4 дефекта (per-site в getNoticeState, innerHTML
  с host, неограниченный blockedLog, паттерн exe по домену вместо пути).
- Переработан движок проверки URL: 3 уровня вердикта, разделение
  host/path паттернов, эвристики, понятные объяснения.

### 5.3.0

- Блокировка рекламы (42 домена) и попапов (window.open без жеста).
- Косметическое скрытие баннеров (adblock.js).
- Видимая URL-защита: баннер + блокирующий экран.
- Подсказки на полях пароля.
- 30 советов с фильтром аудитории, 17 угроз.
- Детализация заблокированных трекеров.
- Даты лицензии, динамическая версия в «О плагине».
- Исправлен Duplicate script ID (очередь регистраций).

### 5.2.0

- Срок действия лицензии (начало/конец/остаток).
- Еженедельные напоминания за месяц до истечения.
- Автопонижение до уровня 2 при истечении.
- Per-site уровни защиты.
- Базовый движок проверки URL.

### 5.1.0

- Пул вариаций: 7 776 → 143 360 (ось шума canvas/audio).
- Сайты-исключения (whitelist) с excludeMatches.

### 5.0.0

- Первая версия Manifest V3.
- Синхронная MAIN-world инъекция.
- 5 уровней защиты, лицензирование, DNR-правила.
